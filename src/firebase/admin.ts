// Firebase Admin SDK — NO se usa en producción
// MoralScore usa solo el client SDK + Firestore Security Rules.
// Este archivo queda como placeholder para uso futuro (Cloud Functions, scripts locales).
//
// Para uso local con scripts de seed/migración:
//   1. pnpm add -D firebase-admin
//   2. Configurar env vars FIREBASE_ADMIN_*
//   3. Descomentar el código abajo

export const adminDb = null;
